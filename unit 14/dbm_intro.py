# SQL 

import sqlite3
# is so light that is included in the Python standard library
# no need to install anything extra
# it is a file-based database, so you can create a database by creating a file
# it is a relational database, so you can create tables and relationships between them
# it is a SQL database, so you can use SQL commands to interact with it

